[project]
name = "zuoyizhuo-conversation-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0,<3",
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "uvicorn>=0.30,<1",
]
[project.optional-dependencies]
openai = ["openai>=2.0,<3"]
[tool.pytest.ini_options]
testpaths = ["tests"]
